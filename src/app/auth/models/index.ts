export interface Loginload {
    email: string | null;
    password: string | null;
  }

  export interface StudentLog{
    id: number
    name: string
    lastname: string
    password: string
    age: number
    email: string 
}